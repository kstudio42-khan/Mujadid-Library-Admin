# Mujadid-Library-Admin

Admin Panel for Mujadid Library.

## Phase 1 Objectives:
1. Setup project structure (HTML, CSS, JS).
2. Implement Admin Login (connecting to existing backend).

## Plan:
1. Create `index.html` as the entry point.
2. Create `style.css` for styling.
3. Create `auth.js` for login logic, referencing `gulshan_library/admin-panel/auth.js` and `gulshan_library/auth.js`.
